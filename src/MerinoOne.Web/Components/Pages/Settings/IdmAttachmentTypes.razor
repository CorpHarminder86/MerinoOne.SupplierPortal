@page "/settings/idm-entity-types"
@page "/settings/idm-attachment-types"
@inject NavigationManager Nav

@* R10 — IDM entity-type mappings became Document-kind rows on the unified Outbound Integrations screen
   (Endpoints hub → Outbound). Routes kept as redirects so bookmarks still land. *@

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/integrations/endpoints?tab=outbound", replace: true);
}
