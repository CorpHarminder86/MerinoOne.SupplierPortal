@page "/settings/ln-outbound-endpoints"
@inject NavigationManager Nav

@* R10 — the per-transaction-type LN config screen became the Transaction rows of the unified Outbound
   Integrations panel (Endpoints hub → Outbound). Route kept as a redirect so bookmarks still land. *@

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/integrations/endpoints?tab=outbound", replace: true);
}
