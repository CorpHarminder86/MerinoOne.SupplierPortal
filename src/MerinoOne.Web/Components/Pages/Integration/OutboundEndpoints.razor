@page "/integrations/outbound-endpoints"
@inject NavigationManager Nav

@* R10 — the IDM transport endpoints folded into the unified Outbound Integrations screen (Endpoints hub →
   Outbound; verb/path now live on each Document-kind config row). Route kept as a redirect. *@

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/integrations/endpoints?tab=outbound", replace: true);
}
